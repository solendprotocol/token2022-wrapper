export const WRAPPER_TOKEN_MINT_SEED = Buffer.from("wrapper");
export const RESERVE_AUTHORITY_SEED = Buffer.from("reserve_authority");
export const RESERVE_AUTHORITY_TOKEN_ACCOUNT_SEED = Buffer.from(
  "reserve_authority_token_account"
);
